{
  "name": "prompt-control-deepseek-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "cd pages-gateway && wrangler pages deploy pages --project-name prompt-control-deepseek-api --branch main",
    "test": "node ../tests/deepseek-worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
